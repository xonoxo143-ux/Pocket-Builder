# Next implementation slice

1. Persist workspaces and recent projects across process death.
2. Add a signed toolchain catalog reader and fast launch audit.
3. Add resumable trusted-source downloads with staged verification.
4. Define the foreground build-service protocol and structured build events.
5. Implement the constrained Godot Android export recipe before generic Gradle execution.
6. Run the first full Android compile and repair all compiler findings before merging.
