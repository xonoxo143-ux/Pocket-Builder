# No-Termux policy

PocketBuild does not depend on, invoke, or require the Termux application.

It may study and independently implement general Android/Linux techniques used by terminal environments, such as private executable layouts, process supervision, pipes, environment variables, and package manifests. Any reused third-party source or binary must be license-compatible, attributed, pinned, and verified.
