# Draft pull request notes

This rewrite replaces the original scaffold. It is not ready to merge until the Android project compiles with the pinned toolchain and the source-intent/workspace flows are tested on a real device.
